import imp
import numpy as np
import pandas as pd


def test_function(variable1: np.ndarray, variable2: dict, variable3:list, varible4: int = 0, variable5: str = 'no') -> dict:
    """[summary]

    Args:
        variable1 (np.ndarray): [description]
        variable2 (dict): [description]
        variable3 (list): [description]
        varible4 (int, optional): [description]. Defaults to 0.
        variable5 (str, optional): [description]. Defaults to 'no'.

    Returns:
        dict: [description]
    """
    return variable3